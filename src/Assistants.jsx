import React,{useState} from 'react';
import {PaperPlaneTilt,Sparkle} from '@phosphor-icons/react';
import {platformReply,itemReply} from './island';
export function Assistant({item}){
 const [q,setQ]=useState(''),[log,setLog]=useState([]),[enabled,setEnabled]=useState(true);
 const ask=value=>{if(!value.trim())return;setLog(v=>[...v,{role:'you',text:value.trim()},{role:'assistant',text:item&&!enabled?'消息仅保存在当前演示中，尚未发送给真实卖家。':item?itemReply(value,item):platformReply(value)}]);setQ('')};
 return <div className="assistant"><div className="assistant-heading"><Sparkle size={22}/><div><b>{item?'物品信息助手':'平台 AI 助手'}</b><p>{item?'仅引用卖家确认的信息，不代卖家承诺。':'发布、交接与历史来源，在这里了解。'}</p></div></div><p className="fine-print">当前为规则问答演示，未连接语言模型或真实聊天服务。</p>{item&&<label className="checkbox-line"><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/>让物品助手回答已确认的信息</label>}<div className="quick-questions">{(item?['多少钱？','状况和缺陷？','在哪里交接？']:['怎么发布？','跨校怎么交接？','转手记录怎么看？']).map(s=><button className="outline-button" key={s} disabled={!enabled} onClick={()=>ask(s)}>{s}</button>)}</div><div className="assistant-log" aria-live="polite">{log.map((m,i)=><div className={`assistant-message ${m.role}`} key={i}><small>{m.role==='you'?'我':item?'物品助手 · 卖家资料':'平台助手 · 使用说明'}</small><p>{m.text}</p></div>)}</div><form className="chat-compose" onSubmit={e=>{e.preventDefault();ask(q)}}><input aria-label="向助手提问" placeholder={enabled?'输入你的问题…':'给卖家留言（本地演示）'} value={q} onChange={e=>setQ(e.target.value)}/><button className="btn purple" disabled={!q.trim()} aria-label="发送给助手"><PaperPlaneTilt size={20}/></button></form></div>
}
